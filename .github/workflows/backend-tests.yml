# name: Backend Tests

# on:
#   pull_request:
#     branches: [main]
#   push:
#     branches: [main]

# jobs:
#   backend-tests:
#     runs-on: ubuntu-latest
#     timeout-minutes: 10

#     services:
#       postgres:
#         image: postgres:16-alpine
#         env:
#           POSTGRES_USER: postgres
#           POSTGRES_PASSWORD: postgres
#           POSTGRES_DB: agile_party_test
#         options: >-
#           --health-cmd pg_isready
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5
#         ports:
#           - 5432:5432

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Go
#         uses: actions/setup-go@v5
#         with:
#           go-version: '1.21'
#           cache-dependency-path: backend/go.sum

#       - name: Run database migrations
#         working-directory: backend
#         run: |
#           for migration in internal/adapters/postgres/migrations/*.sql; do
#             PGPASSWORD=postgres psql -h localhost -U postgres -d agile_party_test -f "$migration"
#           done

#       - name: Run Go tests
#         working-directory: backend
#         env:
#           DB_HOST: localhost
#           DB_PORT: 5432
#           DB_USER: postgres
#           DB_PASSWORD: postgres
#           DB_NAME: agile_party_test
#           DB_SSLMODE: disable
#         run: go test -v -race -coverprofile=coverage.out -covermode=atomic ./...

#       - name: Generate coverage report
#         working-directory: backend
#         run: go tool cover -func=coverage.out

#       - name: Upload coverage to Codecov
#         uses: codecov/codecov-action@v4
#         with:
#           file: backend/coverage.out
#           flags: backend
#           fail_ci_if_error: false
