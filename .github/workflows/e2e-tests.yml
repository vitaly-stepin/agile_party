# name: E2E Tests

# on:
#   pull_request:
#     branches: [main]
#   push:
#     branches: [main]

# jobs:
#   e2e-tests:
#     runs-on: ubuntu-latest
#     timeout-minutes: 20

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Start E2E services
#         run: docker compose -f e2e/docker-compose.e2e.yml up -d

#       - name: Wait for services
#         run: sleep 15

#       - name: Health check - Frontend
#         run: |
#           curl --retry 10 --retry-delay 2 --retry-connrefused http://localhost:5174

#       - name: Health check - Backend
#         run: |
#           curl --retry 10 --retry-delay 2 --retry-connrefused http://localhost:8081/api/health

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'
#           cache-dependency-path: e2e/package-lock.json

#       - name: Install E2E dependencies
#         working-directory: e2e
#         run: npm ci

#       - name: Install Playwright browsers
#         working-directory: e2e
#         run: npx playwright install --with-deps chromium

#       - name: Run E2E tests
#         working-directory: e2e
#         env:
#           E2E_BASE_URL: http://localhost:5174
#         run: npm run test

#       - name: Upload Playwright report
#         if: failure()
#         uses: actions/upload-artifact@v4
#         with:
#           name: playwright-report
#           path: e2e/playwright-report/
#           retention-days: 30

#       - name: Upload test results
#         if: failure()
#         uses: actions/upload-artifact@v4
#         with:
#           name: test-results
#           path: e2e/test-results/
#           retention-days: 30

#       - name: Stop services
#         if: always()
#         run: docker compose -f e2e/docker-compose.e2e.yml down -v
